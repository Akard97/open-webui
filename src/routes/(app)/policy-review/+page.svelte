<!--
  Policy Review — placeholder.

  The original Policy Review tool (FastAPI service on port 8100, embedded
  via iframe) was removed pending a full rebuild. This route stays so the
  rail entry in src/lib/components/app/railItems.ts still has a target;
  when the new tool lands, swap the body for its real UI.
-->
<svelte:head>
	<title>Policy Review · Osool</title>
</svelte:head>

<div class="w-full h-full" style="background:#000;"></div>
